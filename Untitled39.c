#include<stdio.h>
int main()

{
    int HoursStudied = 60;  //10 hours or more
    int KidsBeatUp = 0; //0 kid

    if( (HoursStudied >= 10) && (KidsBeatUp == 0) ){
        printf("you are a good student");
    }

    return 0;
}
